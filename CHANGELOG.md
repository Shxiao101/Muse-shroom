# 更新记录

每个版本一行，说这一版实际改了什么。版本号同时写在 `pyproject.toml`、`src/muse_shroom/__init__.py` 和 README 标题里，三处必须一致。

## 0.13.2

搜索算法整体退回 0.12.10，代码和 0.12.10 完全相同，0.13.0、0.13.1 的改动都撤掉了。

撤掉的原因是 0.13 没有比 0.12.10 好。在维护者亲手标注过的 8 个需求上回放，0.12.10 的候选池里有 14 个标为有用的仓库，0.13.1 只有 11 个，短名单里两者一样多。后来试的几种改法，跟随列表 README 里的链接、用候选池里的 topic 换查询、调整机制词的查询顺序，都没有超过 0.12.10。

## 0.13.1

短名单退回 0.12.10 的选法，0.13.0 的其余改动保留。0.13.0 让所有类别同场争每一个位置，结果有两种表现。一种是同一个机制的仓库占掉大半个名单，固定候选池上机制重复率从 0.34 涨到 0.72。另一种出现在带搜索来源的真实候选池里，从来不占位置的 popular 类拿走了核心之外的全部九个位置，对题但只有几十星的仓库一个都进不来。现在每个类别又有自己的位置，名字、描述或 topics 就能看出相关的仓库，先于只在 README 里提到的仓库入选。

选短名单快了很多。每个候选的名字、描述和 README 只规范化一次，不再为每个词重做一遍。531 个候选的固定池上，一次选择从八秒多降到一秒左右。

## 0.13.0

这一版改的是搜索算法本身，调用次数的上限一条没加。

词面匹配按整词算。`rust` 不再命中 `trust`，`git` 不再命中 `digital`。仓库名里的 `/`、`-`、`_`、`.` 当作分隔符，`C++` 和 `C#` 分得开，中文仍按连续文本匹配。

短名单不再让只在名字、描述里匹配上的候选先把名额占满，README 里证据强的仓库也能进。核心概念最多留三个位置，其余位置统一按分数竞争，已经选进来的机制再出现会被扣分。

deep 模式每轮的 README 分两批取。第一批按概念和方向轮流各取一个，第二批先补还没被 README 证实的方向。请求失败的这一轮不再重试，留到下一轮。quick 模式仍是一次取完。

deep 模式每轮的查询也分两批。第二批按第一批的实际产出排序，先是还没有证据的目标方向，再是没试过的查询，然后是产出高的。网络失败不算作零产出。

判断一轮有没有进展，改看 README 证据。新仓库的 README 覆盖了之前没覆盖的问题或方向，或者新机制在某个仓库的 README 里找到了出处，才算有进展。换个说法、同一机制的别名、同类仓库都不算。只跑了语义假设、什么也没找到的一轮，也算作没有进展。

迭代时 Agent 给的词，按它指的概念归类，不再按它在列表里排第几。以前第二轮排第二的词会接过第一轮排第二那个词的查询收益，两轮用不同的词找到同一个仓库时，排名只按一个概念算。最严重的是，Agent 给的第一个相邻概念会被当成请求里的第一个探索方向，只被这个词找到的仓库，会被记成匹配了用户要的那个方向。

## 0.12.10

muse_supply 第一次读 README 失败、重试时才发现命中排除词的仓库，会从会话里彻底拿掉。以前只从这一次调用的内存里删掉，第一次存下的候选、host_supplied 里的名字、sidecar 里的同一份都还留着，照样能被查看和排名，会话却已经不再报告有东西没取到。

## 0.12.9

被排除词挡掉的仓库，也不能再从语义 sidecar 那边被查看和排名。0.12.6 只筛了回复里展示的部分，sidecar 自己存的那张候选单没筛，rank 恰好从那张单子里选，拿着 README 里的真实引用就能把它排进结果。后来某一轮排除的方向，sidecar 之前在那个方向找到的仓库也一起拿掉。

关系扩展里的反向 README 搜索也守查询额度。它本身就是一次搜索，算进会话的查询总数，却只看关系调用的额度。每轮只剩一条查询时，关键词用掉那一条，它还会接着发。现在额度用完就跳过，查询记录里写明原因。

muse_supply 读 README 失败，再补交一次成功了，会话就不再报告失败。以前第一次失败写进会话以后，后面的调用一律沿用，quick 会话又没有下一轮来重算，这个失败就一直挂着。搜索或迭代自己留下的失败照旧保留。

sidecar 查到的 release 如果来自旧缓存，会话也记成过期。0.12.5 补了 README 这一路，release 查询自己报回来的状态还是被丢掉了。

同一会话第三次排名时，第一次展示过的机制不会再算成新增。0.12.7 改成读之前的排名，可只读了最近一份，先 A、再 B、再 A，A 就被算了两次。现在读这个会话每一次排名选过的机制。

「看过的仓库」里的最近日期是最近一次展示的日期。0.12.7 把同一会话里重复展示的记录合成一条，日期也跟着停在了第一次。

Explorer 看历史阶段时，结果页和未探索页顶部的「← 概览」会留在同一个阶段。概览页的两张跳转卡片、从图谱点进仓库再返回，也一样。0.12.8 只修了页面中间那排导航。

换了配置的进程接手会话，一轮结束时也按会话自己的额度算。0.12.6 只改了入口。会话建的时候允许五轮，接手的进程只配了一轮，这一轮能跑，跑完却按一轮记成用完，observe 显示还剩四轮，can_iterate 却是 false。每轮读 README 的篇数、复用旧会话时报告的额度也一样改了。

## 0.12.8

端口上如果是别的服务，rank 不会再因此失败。那个服务只要在 /api/meta 返回一段合法但不是对象的 JSON，取字段就会抛异常——排名其实已经存好了，却倒在准备浏览器链接这一步，用户什么都拿不到。

Explorer 里看历史阶段时，页面之间跳转不再丢掉阶段。选了「初始」再点结果或未探索，以前会不声不响地显示最终状态。

历史阶段用的是它自己那一轮的请求。每次迭代都会改写会话里的请求，所以早先那一轮的探索方向和图谱里，会出现后来才加进去的方向。现在每个快照存下当时的请求。

## 0.12.7

同一个会话的两次调用不再互相覆盖。宿主在线程里跑同步工具，每次调用各开一份存储，所以两次 muse_supply 会读到同一份旧状态、后写的那次把先写的抹掉——两个仓库都存下来了，会话里却只记得一个，它自己的上限检查从此不准。现在同一会话的写操作排队进行。

已经给用户看过的仓库不会被后来的清单抹掉。展示历史原本是从「当前排名」里读的，一个会话一行，所以深搜里 rank 完再迭代再 rank，第一份清单就被忘掉了，里面的仓库又变回「没给过」。现在单独累计，旧库里的排名会一次性搬过来。

短名单不再算作「已经展示过的机制」。短名单是交给 Agent 评估的，不是给用户看的结果——以前第一次真的选中某个机制时，它反而不算新增；连一条都没推荐的排名也会报告展示过机制。

## 0.12.6

排除词对每一条进入会话的路都生效。正常召回一直会在读完 README 之后再筛一遍，而 muse_supply 只看元数据、语义 sidecar 根本不筛——名字和描述都干净、README 明写是课程的仓库，从这两扇门都能进来。

code 检索和关键词检索共用同一份查询额度。以前关键词那一步守着每轮的上限，code 那一步接着发，两者却都计入同一个总数：两轮下来会比会话预算多花四条。expand 那条固定的十次也归进了会话预算。

恢复会话时用这个会话自己的限制。observe 读的是存下来的额度，iterate 读的是当前进程的默认值，所以配置不同的两次调用会一个说不能继续、另一个照样继续。

## 0.12.5

中途失败的搜索不再被当成完成的结果复用。以前一个会话刚建出来就和「已完成」长得一模一样，所以搜到一半断了之后，相同请求会拿到那个空会话，而且不再请求网络。响应体被截断也归入正常的网络重试。

release 请求失败后可以重试。上一版修的是 README 那个分支，旁边这个同样形状的分支漏了。

supply 和 sidecar 取到的过期证据会写回会话。以前只在那一次回复里说一声，接下来的 observe 和 rank 读会话，又报成新鲜的。

## 0.12.4

README 请求失败后可以重试。以前一次网络抖动就把那个仓库的 README 记成空的，和「这个仓库本来就没有 README」分不出来，之后整个会话都不再问第二次，这个候选就永远没有可核验的证据了。

一个会话不能迭代的时候，`muse_iterate` 不再假装能。快搜会话和已经主动停下的深搜会话，以前直接调 iterate 仍然会多发一轮查询，虽然 observe 已经说了不行。

## 0.12.3

排除词按完整词匹配。以前排除 `course` 会把 discourse/discourse 这样只是名字里碰巧带了这几个字母的仓库一起筛掉。

换了数据目录之后，不再把链接指向别的库。占着端口的 Explorer 如果服务的是另一个数据目录，这次搜索会另起一个。

「再找一些」不再被指引挡住。同一需求在排完序之后再搜一次，会开一个新会话，把已经给过的仓库让位给没看过的。

README 重写成给普通用户看的；`examples/` 两个示例 JSON 不再随产品发布；加上 `CONTRIBUTING.md` 和本文件。

## 0.12.2

候选池填满之后，后面的查询不再整条落空：它们付过的调用照样记账，返回的名次也照样算进已有候选的证据。

## 0.12.1

宿主报错仓库名时给出正确的拼法，并在回复里说明这一轮实际生效的规则。

## 0.12.0

给「以前给你看过」的仓库补上链接，Explorer 里也能单独列出这份记录。

## 0.11.1

只从 sidecar 来的候选也能取证；MCP 回复压进宿主的输出上限内。

## 0.11.0

记住已经展示给用户的仓库，同一需求再搜时不再重复推荐。

## 0.10.1

砍掉一次 Muse-shroom 运行在启动时加载的上下文。

## 0.10.0

完整保留宿主自己那份草稿，精简发给 Agent 的回复，把失败的查询单独隔离出来。

## 0.9.0

跨领域假设改成能真正够到结果；宿主召回进入 boundary 流程；wildcard 判定收紧。

## 0.8.1

GitHub 网络抖动时重试一次。

## 0.8.0

rank 之前先做宿主召回；盲评只在 A/B 里进行。

## 0.7.1

Skill 改成显式触发，并更名为 muse-shroom。

## 0.7.0

选择权交给 Agent，删掉由模型自己写的发布闸门。

## 0.7.0 以前

0.3 到 0.6 是早期版本：搜索内核、两步确认、boundary 基础、有界的迭代循环、MCP 适配层和只读 Explorer 都在这段时间成形。逐条改动见 git 历史。
